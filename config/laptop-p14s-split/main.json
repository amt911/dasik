{
  "metadata": {
    "note": "Split form of the ThinkPad P14s Gen 2 (AMD) config. Secrets live in secrets/ (gitignored): copy the .example files and fill them in — the LUKS passphrase is referenced by BOTH partitions from one file, so they cannot drift apart. Everything else is the same config as the single-file form."
  },
  "timezone": {
    "region": "Europe",
    "city": "Madrid"
  },
  "locales": {
    "$include": "locales.json"
  },
  "pacman": {
    "options": {
      "Parallel": true,
      "Color": true,
      "VerbosePkgLists": false
    },
    "multilib": true
  },
  "users": {
    "$include": "users.json"
  },
  "packages": {
    "$include": "packages.json"
  },
  "systemd": {
    "$include": "systemd.json"
  },
  "initramfs": "dracut",
  "bluetooth": {
    "enable": true,
    "in_initramfs": false
  },
  "disks": {
    "$include": "disks.json"
  },
  "udev_rules": [
    {
      "name": "1-qudelix.rules",
      "content": {
        "$include_text": "udev_rules/1-qudelix.rules"
      }
    },
    {
      "name": "50-dolphin-UB400.rules",
      "content": {
        "$include_text": "udev_rules/50-dolphin-UB400.rules"
      }
    }
  ],
  "modprobe_conf": [
    {
      "name": "nested_virt.conf",
      "content": {
        "$include_text": "modprobe.d/nested_virt.conf"
      }
    }
  ],
  "modules_load": [],
  "files": [
    {
      "path": "/etc/plasmalogin.conf.d/10-dasik.conf",
      "content": {
        "$include_text": "files/etc-plasmalogin.conf.d-10-dasik.conf"
      }
    },
    {
      "path": "/etc/pam.d/sudo",
      "content": {
        "$include_text": "files/etc-pam.d-sudo"
      }
    },
    {
      "path": "/etc/pam.d/polkit-1",
      "content": {
        "$include_text": "files/etc-pam.d-polkit-1"
      }
    },
    {
      "path": "/etc/pam.d/plasmalogin",
      "content": {
        "$include_text": "files/etc-pam.d-plasmalogin"
      }
    }
  ],
  "zram": {
    "$include": "zram.json"
  },
  "firewall": {
    "$include": "firewall.json"
  },
  "bootloader": "sd-boot",
  "enable_microcode": true,
  "snapper": {
    "$include": "snapper.json"
  },
  "hostname": "archlinux-p14s",
  "network": {
    "type": "NetworkManager",
    "add_default_hosts": true
  },
  "drivers": [
    "amd"
  ],
  "hardware_acceleration": {
    "enable": true,
    "install_codecs": true
  },
  "enable_trim": true,
  "kernel_cmdline": [
    "rd.luks.name=0ed69442-a99e-5f65-88d8-28d3e9945408=cryptroot",
    "rd.luks.name=7c678858-646c-5261-9727-4779b680f718=cryptswap",
    "resume=/dev/mapper/cryptswap"
  ],
  "notes": "Lenovo ThinkPad P14s Gen 2 (AMD Ryzen 7 PRO 5850U, Cezanne iGPU, no discrete GPU). BEFORE APPLYING: (1) set both luks_password fields (they are placeholders — use the SAME passphrase on cryptroot and cryptswap so systemd's initrd password cache asks only once); (2) confirm the disk is /dev/nvme0n1 with lsblk — wipe_disk is true; (3) size the swap partition to your RAM (32GiB assumed) or hibernation cannot fit; (4) regenerate hashed_password with `dasik hash-password`. In the UEFI set Sleep to 'Linux' (s2idle) — with 'Windows 10' the lid switch is flaky. Fingerprint: Synaptics 06cb:00bd, supported in-tree; enrol with `fprintd-enroll` and keep the reader firmware current via fwupd."
}
