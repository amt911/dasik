# One line: the LUKS passphrase. BOTH encrypted partitions read this same
# file, so root and swap can never drift apart and systemd's initrd password
# cache asks for it only once.
change-me-before-applying
