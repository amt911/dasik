# Managed by dasik. Plasma Login Manager (plasmalogin.service) reads
# /etc/plasmalogin.conf.d/*.conf — it is an SDDM fork, but it does NOT read
# /etc/sddm.conf.d and it is fixed to the Breeze theme (no [Theme] key).
# The previous sddm.conf.d "DisplayServer=x11-user" is intentionally NOT carried
# over: plasmalogin defaults to Wayland. Re-add it only if you want X11 again.
[Autologin]
Relogin=false
Session=plasma
User=andres

[General]
HaltCommand=/usr/bin/systemctl poweroff
RebootCommand=/usr/bin/systemctl reboot

[Users]
MaximumUid=60513
MinimumUid=1000
