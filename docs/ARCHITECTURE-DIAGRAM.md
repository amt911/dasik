# Arquitectura Visual - Sistema Idempotente

## 📐 Diagrama de Componentes

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ENTRADA: config.json                         │
│  {                                                                   │
│    "timezone": {"region": "Europe", "city": "Madrid"},              │
│    "locales": {...},                                                 │
│    "network": {...},                                                 │
│    "disks": {...}  ← opcional                                        │
│  }                                                                   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      actions_handler_v2.py                           │
│                                                                      │
│  setup_actions()  ← Registra todas las acciones disponibles         │
│  execute_installation(config_file)                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        ActionRegistry                                │
│                                                                      │
│  [                                                                   │
│    {action: DiskPartition, key: "disks", optional: True},          │
│    {action: Timezone, key: "timezone", optional: True},             │
│    {action: Locale, key: "locales", optional: True},                │
│    {action: Network, key: "network", optional: True},               │
│    {action: BaseInstall, key: "__root__", optional: False}          │
│  ]                                                                   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        ActionExecutor                                │
│                                                                      │
│  Para cada acción registrada:                                       │
│    1. ✓ Validar configuración                                       │
│    2. 🔍 action.is_needed() ← IDEMPOTENCIA                          │
│    3. ⚙️  Si True → action.execute()                                 │
│    4. ✓ action.verify()                                             │
│                                                                      │
│  Tracking: [executed], [skipped], [not_needed], [failed]           │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     AbstractAction (base)                            │
│                                                                      │
│  @property name → str                                               │
│  is_needed() → bool    ← Verifica estado actual del sistema         │
│  execute() → None      ← Aplica cambios                             │
│  verify() → bool       ← Valida que funcionó                        │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
          ┌──────────────────┴──────────────────┐
          │                                     │
          ▼                                     ▼
┌──────────────────────┐           ┌──────────────────────┐
│  TimezoneAction      │           │  LocaleAction        │
│                      │           │                      │
│  is_needed():        │           │  is_needed():        │
│   ¿/etc/localtime    │           │   ¿locale.gen OK?    │
│    apunta a correcto?│           │   ¿locale.conf OK?   │
│                      │           │                      │
│  execute():          │           │  execute():          │
│   ln -sf timezone    │           │   Generar locales    │
│   hwclock --systohc  │           │   Configurar tty     │
└──────────────────────┘           └──────────────────────┘
          │                                     │
          ▼                                     ▼
┌──────────────────────┐           ┌──────────────────────┐
│  NetworkAction       │           │  DiskPartitionAction │
│                      │           │                      │
│  is_needed():        │           │  is_needed():        │
│   ¿hostname OK?      │           │   ¿particiones OK?   │
│   ¿/etc/hosts OK?    │           │   ¿filesystems OK?   │
│                      │           │                      │
│  execute():          │           │  execute():          │
│   Set hostname       │           │   Particionar disco  │
│   Configure hosts    │           │   Formatear          │
└──────────────────────┘           └──────────────────────┘
```

## 🔄 Flujo de Ejecución

```
                    [INICIO]
                       │
                       ▼
         ┌─────────────────────────┐
         │ Cargar config.json      │
         └────────────┬────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ Para cada acción:       │
         └────────────┬────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ ¿Config válida?         │
         └────────┬────────┬───────┘
                  │No      │Sí
                  ▼        ▼
         ┌──────────┐   ┌──────────────────┐
         │ SKIP     │   │ is_needed()?     │
         └──────────┘   └────┬─────────┬───┘
                             │No       │Sí
                             ▼         ▼
                    ┌────────────┐  ┌──────────┐
                    │ SKIP       │  │ execute()│
                    │ (already   │  └────┬─────┘
                    │ configured)│       │
                    └────────────┘       ▼
                             │    ┌──────────┐
                             │    │ verify() │
                             │    └────┬─────┘
                             │         │
                             ▼         ▼
                      ┌──────────────────┐
                      │ Registrar resultado│
                      └────────┬──────────┘
                               │
                               ▼
                      ┌──────────────────┐
                      │ ¿Hay más acciones?│
                      └────┬─────────┬───┘
                           │Sí       │No
                           │         ▼
                           │    ┌─────────────┐
                           │    │ Mostrar     │
                           │    │ resumen     │
                           │    └─────────────┘
                           │         │
                           └─────────┤
                                     ▼
                                  [FIN]
```

## 💡 Ejemplo de Flujo Completo

### Primera Ejecución (Sistema Limpio)

```
config.json:
{
  "timezone": {"region": "Europe", "city": "Madrid"},
  "hostname": "my-arch",
  "enable_microcode": true
}

┌─────────────────────────────────────────────────────────┐
│ TIMEZONE CONFIGURATION                                  │
├─────────────────────────────────────────────────────────┤
│ ✓ Config válida                                         │
│ 🔍 is_needed() → True (/etc/localtime no existe)        │
│ ⚙️  execute() → ln -sf .../Europe/Madrid                │
│ ✓ verify() → True                                       │
│ ✅ SUCCESS                                               │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ BASE INSTALLATION                                       │
├─────────────────────────────────────────────────────────┤
│ ✓ Config válida                                         │
│ 🔍 is_needed() → True (no hay sistema instalado)        │
│ ⚙️  execute() → pacstrap base linux...                  │
│ ✓ verify() → True                                       │
│ ✅ SUCCESS                                               │
└─────────────────────────────────────────────────────────┘

RESULTADO FINAL:
✅ Successfully executed:
   • Timezone Configuration
   • Base Installation
```

### Segunda Ejecución (Sistema Ya Configurado)

```
Mismo config.json

┌─────────────────────────────────────────────────────────┐
│ TIMEZONE CONFIGURATION                                  │
├─────────────────────────────────────────────────────────┤
│ ✓ Config válida                                         │
│ 🔍 is_needed() → False (/etc/localtime ya apunta bien)  │
│ ⏭️  SKIP - Already configured                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ BASE INSTALLATION                                       │
├─────────────────────────────────────────────────────────┤
│ ✓ Config válida                                         │
│ 🔍 is_needed() → False (sistema ya instalado)           │
│ ⏭️  SKIP - Already configured                           │
└─────────────────────────────────────────────────────────┘

RESULTADO FINAL:
ℹ️  Already configured (idempotent):
   • Timezone Configuration
   • Base Installation

System already configured - no changes needed
```

### Tercera Ejecución (Config Modificada)

```
config.json (MODIFICADO):
{
  "timezone": {"region": "America", "city": "New_York"},  ← CAMBIO
  "hostname": "my-arch",
  "enable_microcode": true
}

┌─────────────────────────────────────────────────────────┐
│ TIMEZONE CONFIGURATION                                  │
├─────────────────────────────────────────────────────────┤
│ ✓ Config válida                                         │
│ 🔍 is_needed() → True (timezone cambió)                 │
│ ⚙️  execute() → ln -sf .../America/New_York             │
│ ✓ verify() → True                                       │
│ ✅ SUCCESS                                               │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ BASE INSTALLATION                                       │
├─────────────────────────────────────────────────────────┤
│ ✓ Config válida                                         │
│ 🔍 is_needed() → False (sistema ya instalado)           │
│ ⏭️  SKIP - Already configured                           │
└─────────────────────────────────────────────────────────┘

RESULTADO FINAL:
✅ Successfully executed:
   • Timezone Configuration  ← ACTUALIZADO

ℹ️  Already configured (idempotent):
   • Base Installation
```

## 🧩 Componentes y Responsabilidades

| Componente | Responsabilidad | Entradas | Salidas |
|------------|----------------|----------|---------|
| **actions_handler_v2** | Punto de entrada, registrar acciones | config.json | bool (success) |
| **ActionRegistry** | Mantener lista de acciones disponibles | action_class, config_key | action metadata |
| **ActionExecutor** | Orquestar ejecución con idempotencia | config dict | ActionResult[] |
| **AbstractAction** | Base para todas las acciones | config, context | - |
| **TimezoneAction** | Configurar timezone | timezone config | - |
| **LocaleAction** | Configurar locales | locale config | - |
| **NetworkAction** | Configurar red | network config | - |
| **DiskPartitionAction** | Particionar disco | disk config | partition_map |
| **BaseInstallAction** | Instalar sistema base | root config | - |
| **ActionContext** | Compartir estado entre acciones | key, value | value |

## 🎯 Principios de Diseño

1. **Single Responsibility**: Cada acción hace una cosa
2. **Open/Closed**: Abierto a extensión, cerrado a modificación
3. **Dependency Inversion**: Depende de abstracciones, no implementaciones
4. **Idempotencia**: Ejecutar N veces = ejecutar 1 vez
5. **Fail Fast**: Validar antes de ejecutar
6. **Separation of Concerns**: Verificación separada de ejecución

## 🔐 Garantías del Sistema

✅ **Idempotencia**: Ejecutar múltiples veces con mismo config es seguro
✅ **Validación**: Config se valida antes de cualquier cambio
✅ **Atomicidad**: Cada acción es independiente
✅ **Trazabilidad**: Todos los resultados se registran
✅ **Extensibilidad**: Nuevas acciones sin tocar código existente
✅ **Retrocompatibilidad**: API antigua sigue funcionando
