{
  "metadata": {
    "note": "PLAN v3 Task 9 scenario A: `pacman.repositories` + `pacman.keys` for the author's real `[amt911]` repo, exercised through EVERY verb (install-driven for the chroot path, then guest-pacman-repo.sh for --target /). `config-saver` is in `packages` with NO `package_sources` entry: it must resolve straight from `[amt911]`, which means PacmanRepositoriesAction has to converge (key trusted, section written, database synced) in the SAME apply, before PackagesAction ever runs pacman -S. NOT for real hardware. Needs NETWORK: pacman syncs the real https://amt911.github.io/arch-packages/$arch during install. Autologins root on ttyS0 and ships python-pydantic/python-colorama so dasik runs from the 9p-mounted repo for the guest driver.",
    "disk": "12G: config-saver plus its own dependency tree needs more room than the 8G minimal configs."
  },
  "disks": {
    "disks": [
      {
        "device": "/dev/vda",
        "partition_table": "gpt",
        "wipe_disk": false,
        "partitions": [
          {
            "label": "ESP",
            "size": "512MiB",
            "filesystem": "fat32",
            "partition_type": "esp",
            "mountpoint": "/boot",
            "format": true
          },
          {
            "label": "ROOT",
            "size": "rest",
            "filesystem": "ext4",
            "partition_type": "linux",
            "mountpoint": "/",
            "format": true
          }
        ]
      }
    ]
  },
  "enable_microcode": false,
  "packages": [
    "base",
    "linux",
    "linux-firmware",
    "sudo",
    "git",
    "python",
    "python-pydantic",
    "python-colorama",
    "config-saver"
  ],
  "pacman": {
    "repositories": [
      {
        "name": "amt911",
        "sig_level": "Required",
        "servers": ["https://amt911.github.io/arch-packages/$arch"]
      }
    ],
    "keys": [
      {
        "fingerprint": "6C6568CE34894645A23ABC44B5BD6F8F9023E53B",
        "url": "https://amt911.github.io/arch-packages/amt911.gpg"
      }
    ]
  },
  "bootloader": "sd-boot",
  "hostname": "dasik-pacman-repo",
  "kernel_cmdline": [
    "console=ttyS0,115200"
  ],
  "timezone": {
    "region": "Etc",
    "city": "UTC"
  },
  "locales": {
    "selected_locales": [
      "en_US.UTF-8 UTF-8"
    ],
    "desired_locale": "en_US.UTF-8",
    "desired_tty_layout": "us"
  },
  "network": {
    "type": "systemd-networkd"
  },
  "users": [
    {
      "username": "test",
      "hashed_password": "$6$dasikvmtest$pfoSKqgh8yWKbmkwBpNuvQuJ4/kLeJmwCPCezBolW4pKbtJbtvMGQCM5f7W/hkbF/FmtCMZMYr/NLwDO4OJm7.",
      "groups": [
        "wheel"
      ]
    }
  ],
  "files": [
    {
      "path": "/etc/systemd/system/serial-getty@ttyS0.service.d/autologin.conf",
      "content": "[Service]\nExecStart=\nExecStart=-/sbin/agetty --autologin root --noclear --keep-baud 115200,38400,9600 %I $TERM\n"
    }
  ]
}
